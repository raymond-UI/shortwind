// Placeholder. Issue #19 lands the real pipeline that copies recipes into
// apps/web/public/registry/. Until then this is a no-op so apps/web's build
// script can run end-to-end.
console.log("[registry] build placeholder — see docs/issues/19 for the real pipeline.");
