{
  "name": "@shortwind/core",
  "version": "0.1.0-beta.25",
  "description": "Parser, resolver, and expand() — zero Tailwind dependency. Used by every other Shortwind package and the CDN runtime expander.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src",
    "prepublishOnly": "pnpm run build"
  },
  "dependencies": {
    "tailwind-merge": "^3.6.0"
  },
  "publishConfig": {
    "access": "public",
    "tag": "beta"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/raymond-UI/shortwind.git",
    "directory": "packages/core"
  },
  "homepage": "https://shortwind.dev",
  "bugs": {
    "url": "https://github.com/raymond-UI/shortwind/issues"
  }
}
